---
export interface Props {
    icon: { width: number; height: number; svgPathData: string; };
}
const { icon } = Astro.props;
---

<svg xmlns="http://www.w3.org/2000/svg" viewBox={`0 0 ${icon.width} ${icon.height}`}>
    <path d={icon.svgPathData}/>
</svg>